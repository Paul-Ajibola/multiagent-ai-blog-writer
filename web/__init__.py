# holds everythin HTTP realated like the routes, SSE serialization and request schema,
# while the agent folder holders everything LangGraph/LLM-related (schema, prompts, nodes, graph wiring and persistence)
